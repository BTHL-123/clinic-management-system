# ===== CẤU HÌNH FRONTEND =====
# Copy file này thành .env rồi điền giá trị phù hợp.
# File .env sẽ KHÔNG được đưa lên Git (đã có trong .gitignore).

# Google OAuth Client ID (lấy từ Google Cloud Console)
VITE_GOOGLE_CLIENT_ID=
